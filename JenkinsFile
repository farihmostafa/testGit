pipeline {
   agent any

   stages {
      stage('Build') {
         steps {
            echo 'Hello Build'
         }
      }
      stage('test') {
         steps {
            echo 'Hello test'
         }
      }
   }
}